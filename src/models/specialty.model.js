export default class User{
	constructor(name,email,password,confirmation_token){
		this.name = name,
		this.email = email,
		this.password = password,
		this.confirmation_token = confirmation_token
	}
}