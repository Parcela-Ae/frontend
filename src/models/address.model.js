export default class Address {
    constructor(cep, street, uf, city, district, number, complement) {
        this.cep = cep,
        this.street = street,
        this.uf = uf,
        this.city = city,
        this.district =district,
        this.number = number,
        this.complement = complement
	}
}
